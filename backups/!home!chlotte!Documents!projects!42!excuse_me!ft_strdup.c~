#include "tools.h"

char	*ft_strdup(const char *src)
{
    int		len;
    char	*dst;
    
    len = ft_strlen(src);
    if (!(dst = (char*)malloc(len + 1)))
	return (NULL);
    dst[len] = '\0';
    while (len-- > 0)
	dst[len] = src[len];
    return (dst);
}
