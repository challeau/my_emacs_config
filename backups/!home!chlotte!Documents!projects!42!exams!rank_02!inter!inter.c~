#include <unistd.h>

size_t	ft_strlen(const char *str){
	size_t i = 0;
	while (str[i])
		i++;
	return (i);
}

int	ft_strcmp(const char *str, char c){
	size_t i = 0;
	while (str[i]){
		if (str[i] == c)
			return (i);
		i++;
	}
	return (-1);
}
