FROM Ubuntu	               --> every docker image should be based on another
     			           image: that of an OS or that of a previously created
			           image based off an OS.
			       	   All Dockerfiles must start with a FROM instruction.

RUN apt-get update
RUN apt-get install python     --> install dependencies.

COPY . /opt/source-code        --> copies files from the local system to the image.

ENTRYPOINT FLASK_APP=/opt/source-code/app.py flask run
	   		       --> Specifies the command that will be run as the
			       	   image is run as a container.
