#include "libft.h"

char	*ft_add_char(char *src, char to_add){
	size_t i = 0;
	char *new = ft_calloc(ft_strlen(src) + 2);

	while (src[i]){
		new[i] = src[i];
		i++;
	}
	new[i] = to_add;
	new[i + 1] = '\0';
	ft_memdel(src);
	return (new);
}
