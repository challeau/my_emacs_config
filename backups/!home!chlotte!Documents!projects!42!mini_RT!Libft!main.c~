#include <stdio.h>
#include <stdlib.h>
#include "libft.h"
int	main(__attribute__((unused))int ac, __attribute__((unused))char **av)
{
	char *str = ft_strdup("will u get cut");

	str = realloc(str, 6);
	printf("%s\n", str);
	return (0);
}
