#include "minirt.h"

int	ft_abs(int num){
	return(num < 0 ? -num : num);
}

int	ft_floor(float value){
	return ((int)value);
}

float	ft_mod(float x, float y){
	float res;

	res = x - y * ft_floor(x / y);
	return (res);
}
