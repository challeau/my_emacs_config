#include "tools.h"

int	ft_intlen(long n)
{
    long nb;
    int len;

    nb = n;
    len = 1;
    if (nb < 0)
    {
	nb *= -1;
	len++;
    }
    while (nb >= 10)
    {
	nb /= 10;
	len++;
    }
    return (len);
}
