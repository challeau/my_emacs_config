#include "cub3d.h"

bool	ft_init_window(t_mlx *m, uint16_t width, uint16_t height, char *title)
{
	m->mlx_ptr = mlx_init();
	m->win_ptr = mlx_new_window(m->mlx_ptr, width, height, title);
	if (m->win_ptr == NULL)
		return (false);
	return (true);
}
