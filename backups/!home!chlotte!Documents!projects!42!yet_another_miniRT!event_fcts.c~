#include "minirt.h"

int	ft_exit(int keycode, void *param)
{
	t_mlx_obj *mlx;

	mlx = param;
	if (keycode == ESC){
		mlx_destroy_window(mlx->mlx_ptr, mlx->win_ptr);
		// free scene somehow
		exit(0);
	}
	return (1);
}
